# Leave empty to use the Vite dev proxy (recommended in development).
# In production set it to your deployed API origin, e.g. https://api.example.com
VITE_API_BASE_URL=

# Where the dev server proxies /api requests during `npm run dev`.
VITE_API_PROXY_TARGET=http://localhost:5000
